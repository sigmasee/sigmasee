// Code generated by ent, DO NOT EDIT.

package entities

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/sigmasee/sigmasee/apex/shared/entities/apexcustomer"
	"github.com/sigmasee/sigmasee/apex/shared/entities/apexcustomeridentity"
	"github.com/sigmasee/sigmasee/apex/shared/entities/predicate"

	"github.com/sigmasee/sigmasee/apex/shared/entities/internal"
)

// ApexCustomerIdentityQuery is the builder for querying ApexCustomerIdentity entities.
type ApexCustomerIdentityQuery struct {
	config
	ctx          *QueryContext
	order        []apexcustomeridentity.OrderOption
	inters       []Interceptor
	predicates   []predicate.ApexCustomerIdentity
	withCustomer *ApexCustomerQuery
	withFKs      bool
	loadTotal    []func(context.Context, []*ApexCustomerIdentity) error
	modifiers    []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ApexCustomerIdentityQuery builder.
func (aciq *ApexCustomerIdentityQuery) Where(ps ...predicate.ApexCustomerIdentity) *ApexCustomerIdentityQuery {
	aciq.predicates = append(aciq.predicates, ps...)
	return aciq
}

// Limit the number of records to be returned by this query.
func (aciq *ApexCustomerIdentityQuery) Limit(limit int) *ApexCustomerIdentityQuery {
	aciq.ctx.Limit = &limit
	return aciq
}

// Offset to start from.
func (aciq *ApexCustomerIdentityQuery) Offset(offset int) *ApexCustomerIdentityQuery {
	aciq.ctx.Offset = &offset
	return aciq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aciq *ApexCustomerIdentityQuery) Unique(unique bool) *ApexCustomerIdentityQuery {
	aciq.ctx.Unique = &unique
	return aciq
}

// Order specifies how the records should be ordered.
func (aciq *ApexCustomerIdentityQuery) Order(o ...apexcustomeridentity.OrderOption) *ApexCustomerIdentityQuery {
	aciq.order = append(aciq.order, o...)
	return aciq
}

// QueryCustomer chains the current query on the "customer" edge.
func (aciq *ApexCustomerIdentityQuery) QueryCustomer() *ApexCustomerQuery {
	query := (&ApexCustomerClient{config: aciq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := aciq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := aciq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(apexcustomeridentity.Table, apexcustomeridentity.FieldID, selector),
			sqlgraph.To(apexcustomer.Table, apexcustomer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, apexcustomeridentity.CustomerTable, apexcustomeridentity.CustomerColumn),
		)
		schemaConfig := aciq.schemaConfig
		step.To.Schema = schemaConfig.ApexCustomer
		step.Edge.Schema = schemaConfig.ApexCustomerIdentity
		fromU = sqlgraph.SetNeighbors(aciq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ApexCustomerIdentity entity from the query.
// Returns a *NotFoundError when no ApexCustomerIdentity was found.
func (aciq *ApexCustomerIdentityQuery) First(ctx context.Context) (*ApexCustomerIdentity, error) {
	nodes, err := aciq.Limit(1).All(setContextOp(ctx, aciq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{apexcustomeridentity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) FirstX(ctx context.Context) *ApexCustomerIdentity {
	node, err := aciq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ApexCustomerIdentity ID from the query.
// Returns a *NotFoundError when no ApexCustomerIdentity ID was found.
func (aciq *ApexCustomerIdentityQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = aciq.Limit(1).IDs(setContextOp(ctx, aciq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{apexcustomeridentity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) FirstIDX(ctx context.Context) string {
	id, err := aciq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ApexCustomerIdentity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ApexCustomerIdentity entity is found.
// Returns a *NotFoundError when no ApexCustomerIdentity entities are found.
func (aciq *ApexCustomerIdentityQuery) Only(ctx context.Context) (*ApexCustomerIdentity, error) {
	nodes, err := aciq.Limit(2).All(setContextOp(ctx, aciq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{apexcustomeridentity.Label}
	default:
		return nil, &NotSingularError{apexcustomeridentity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) OnlyX(ctx context.Context) *ApexCustomerIdentity {
	node, err := aciq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ApexCustomerIdentity ID in the query.
// Returns a *NotSingularError when more than one ApexCustomerIdentity ID is found.
// Returns a *NotFoundError when no entities are found.
func (aciq *ApexCustomerIdentityQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = aciq.Limit(2).IDs(setContextOp(ctx, aciq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{apexcustomeridentity.Label}
	default:
		err = &NotSingularError{apexcustomeridentity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) OnlyIDX(ctx context.Context) string {
	id, err := aciq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ApexCustomerIdentities.
func (aciq *ApexCustomerIdentityQuery) All(ctx context.Context) ([]*ApexCustomerIdentity, error) {
	ctx = setContextOp(ctx, aciq.ctx, "All")
	if err := aciq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ApexCustomerIdentity, *ApexCustomerIdentityQuery]()
	return withInterceptors[[]*ApexCustomerIdentity](ctx, aciq, qr, aciq.inters)
}

// AllX is like All, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) AllX(ctx context.Context) []*ApexCustomerIdentity {
	nodes, err := aciq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ApexCustomerIdentity IDs.
func (aciq *ApexCustomerIdentityQuery) IDs(ctx context.Context) (ids []string, err error) {
	if aciq.ctx.Unique == nil && aciq.path != nil {
		aciq.Unique(true)
	}
	ctx = setContextOp(ctx, aciq.ctx, "IDs")
	if err = aciq.Select(apexcustomeridentity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) IDsX(ctx context.Context) []string {
	ids, err := aciq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aciq *ApexCustomerIdentityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, aciq.ctx, "Count")
	if err := aciq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, aciq, querierCount[*ApexCustomerIdentityQuery](), aciq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) CountX(ctx context.Context) int {
	count, err := aciq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aciq *ApexCustomerIdentityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, aciq.ctx, "Exist")
	switch _, err := aciq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("entities: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (aciq *ApexCustomerIdentityQuery) ExistX(ctx context.Context) bool {
	exist, err := aciq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ApexCustomerIdentityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aciq *ApexCustomerIdentityQuery) Clone() *ApexCustomerIdentityQuery {
	if aciq == nil {
		return nil
	}
	return &ApexCustomerIdentityQuery{
		config:       aciq.config,
		ctx:          aciq.ctx.Clone(),
		order:        append([]apexcustomeridentity.OrderOption{}, aciq.order...),
		inters:       append([]Interceptor{}, aciq.inters...),
		predicates:   append([]predicate.ApexCustomerIdentity{}, aciq.predicates...),
		withCustomer: aciq.withCustomer.Clone(),
		// clone intermediate query.
		sql:  aciq.sql.Clone(),
		path: aciq.path,
	}
}

// WithCustomer tells the query-builder to eager-load the nodes that are connected to
// the "customer" edge. The optional arguments are used to configure the query builder of the edge.
func (aciq *ApexCustomerIdentityQuery) WithCustomer(opts ...func(*ApexCustomerQuery)) *ApexCustomerIdentityQuery {
	query := (&ApexCustomerClient{config: aciq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	aciq.withCustomer = query
	return aciq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ApexCustomerIdentity.Query().
//		GroupBy(apexcustomeridentity.FieldCreatedAt).
//		Aggregate(entities.Count()).
//		Scan(ctx, &v)
func (aciq *ApexCustomerIdentityQuery) GroupBy(field string, fields ...string) *ApexCustomerIdentityGroupBy {
	aciq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ApexCustomerIdentityGroupBy{build: aciq}
	grbuild.flds = &aciq.ctx.Fields
	grbuild.label = apexcustomeridentity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.ApexCustomerIdentity.Query().
//		Select(apexcustomeridentity.FieldCreatedAt).
//		Scan(ctx, &v)
func (aciq *ApexCustomerIdentityQuery) Select(fields ...string) *ApexCustomerIdentitySelect {
	aciq.ctx.Fields = append(aciq.ctx.Fields, fields...)
	sbuild := &ApexCustomerIdentitySelect{ApexCustomerIdentityQuery: aciq}
	sbuild.label = apexcustomeridentity.Label
	sbuild.flds, sbuild.scan = &aciq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ApexCustomerIdentitySelect configured with the given aggregations.
func (aciq *ApexCustomerIdentityQuery) Aggregate(fns ...AggregateFunc) *ApexCustomerIdentitySelect {
	return aciq.Select().Aggregate(fns...)
}

func (aciq *ApexCustomerIdentityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range aciq.inters {
		if inter == nil {
			return fmt.Errorf("entities: uninitialized interceptor (forgotten import entities/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, aciq); err != nil {
				return err
			}
		}
	}
	for _, f := range aciq.ctx.Fields {
		if !apexcustomeridentity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("entities: invalid field %q for query", f)}
		}
	}
	if aciq.path != nil {
		prev, err := aciq.path(ctx)
		if err != nil {
			return err
		}
		aciq.sql = prev
	}
	return nil
}

func (aciq *ApexCustomerIdentityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ApexCustomerIdentity, error) {
	var (
		nodes       = []*ApexCustomerIdentity{}
		withFKs     = aciq.withFKs
		_spec       = aciq.querySpec()
		loadedTypes = [1]bool{
			aciq.withCustomer != nil,
		}
	)
	if aciq.withCustomer != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, apexcustomeridentity.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ApexCustomerIdentity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ApexCustomerIdentity{config: aciq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = aciq.schemaConfig.ApexCustomerIdentity
	ctx = internal.NewSchemaConfigContext(ctx, aciq.schemaConfig)
	if len(aciq.modifiers) > 0 {
		_spec.Modifiers = aciq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aciq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := aciq.withCustomer; query != nil {
		if err := aciq.loadCustomer(ctx, query, nodes, nil,
			func(n *ApexCustomerIdentity, e *ApexCustomer) { n.Edges.Customer = e }); err != nil {
			return nil, err
		}
	}
	for i := range aciq.loadTotal {
		if err := aciq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (aciq *ApexCustomerIdentityQuery) loadCustomer(ctx context.Context, query *ApexCustomerQuery, nodes []*ApexCustomerIdentity, init func(*ApexCustomerIdentity), assign func(*ApexCustomerIdentity, *ApexCustomer)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ApexCustomerIdentity)
	for i := range nodes {
		if nodes[i].apex_customer_identities == nil {
			continue
		}
		fk := *nodes[i].apex_customer_identities
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(apexcustomer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "apex_customer_identities" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (aciq *ApexCustomerIdentityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aciq.querySpec()
	_spec.Node.Schema = aciq.schemaConfig.ApexCustomerIdentity
	ctx = internal.NewSchemaConfigContext(ctx, aciq.schemaConfig)
	if len(aciq.modifiers) > 0 {
		_spec.Modifiers = aciq.modifiers
	}
	_spec.Node.Columns = aciq.ctx.Fields
	if len(aciq.ctx.Fields) > 0 {
		_spec.Unique = aciq.ctx.Unique != nil && *aciq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, aciq.driver, _spec)
}

func (aciq *ApexCustomerIdentityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(apexcustomeridentity.Table, apexcustomeridentity.Columns, sqlgraph.NewFieldSpec(apexcustomeridentity.FieldID, field.TypeString))
	_spec.From = aciq.sql
	if unique := aciq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if aciq.path != nil {
		_spec.Unique = true
	}
	if fields := aciq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, apexcustomeridentity.FieldID)
		for i := range fields {
			if fields[i] != apexcustomeridentity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aciq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aciq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aciq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aciq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aciq *ApexCustomerIdentityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aciq.driver.Dialect())
	t1 := builder.Table(apexcustomeridentity.Table)
	columns := aciq.ctx.Fields
	if len(columns) == 0 {
		columns = apexcustomeridentity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aciq.sql != nil {
		selector = aciq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aciq.ctx.Unique != nil && *aciq.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(aciq.schemaConfig.ApexCustomerIdentity)
	ctx = internal.NewSchemaConfigContext(ctx, aciq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range aciq.modifiers {
		m(selector)
	}
	for _, p := range aciq.predicates {
		p(selector)
	}
	for _, p := range aciq.order {
		p(selector)
	}
	if offset := aciq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aciq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (aciq *ApexCustomerIdentityQuery) ForUpdate(opts ...sql.LockOption) *ApexCustomerIdentityQuery {
	if aciq.driver.Dialect() == dialect.Postgres {
		aciq.Unique(false)
	}
	aciq.modifiers = append(aciq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return aciq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (aciq *ApexCustomerIdentityQuery) ForShare(opts ...sql.LockOption) *ApexCustomerIdentityQuery {
	if aciq.driver.Dialect() == dialect.Postgres {
		aciq.Unique(false)
	}
	aciq.modifiers = append(aciq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return aciq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aciq *ApexCustomerIdentityQuery) Modify(modifiers ...func(s *sql.Selector)) *ApexCustomerIdentitySelect {
	aciq.modifiers = append(aciq.modifiers, modifiers...)
	return aciq.Select()
}

// ApexCustomerIdentityGroupBy is the group-by builder for ApexCustomerIdentity entities.
type ApexCustomerIdentityGroupBy struct {
	selector
	build *ApexCustomerIdentityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (acigb *ApexCustomerIdentityGroupBy) Aggregate(fns ...AggregateFunc) *ApexCustomerIdentityGroupBy {
	acigb.fns = append(acigb.fns, fns...)
	return acigb
}

// Scan applies the selector query and scans the result into the given value.
func (acigb *ApexCustomerIdentityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, acigb.build.ctx, "GroupBy")
	if err := acigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ApexCustomerIdentityQuery, *ApexCustomerIdentityGroupBy](ctx, acigb.build, acigb, acigb.build.inters, v)
}

func (acigb *ApexCustomerIdentityGroupBy) sqlScan(ctx context.Context, root *ApexCustomerIdentityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(acigb.fns))
	for _, fn := range acigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*acigb.flds)+len(acigb.fns))
		for _, f := range *acigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*acigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := acigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ApexCustomerIdentitySelect is the builder for selecting fields of ApexCustomerIdentity entities.
type ApexCustomerIdentitySelect struct {
	*ApexCustomerIdentityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (acis *ApexCustomerIdentitySelect) Aggregate(fns ...AggregateFunc) *ApexCustomerIdentitySelect {
	acis.fns = append(acis.fns, fns...)
	return acis
}

// Scan applies the selector query and scans the result into the given value.
func (acis *ApexCustomerIdentitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, acis.ctx, "Select")
	if err := acis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ApexCustomerIdentityQuery, *ApexCustomerIdentitySelect](ctx, acis.ApexCustomerIdentityQuery, acis, acis.inters, v)
}

func (acis *ApexCustomerIdentitySelect) sqlScan(ctx context.Context, root *ApexCustomerIdentityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(acis.fns))
	for _, fn := range acis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*acis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := acis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (acis *ApexCustomerIdentitySelect) Modify(modifiers ...func(s *sql.Selector)) *ApexCustomerIdentitySelect {
	acis.modifiers = append(acis.modifiers, modifiers...)
	return acis
}
